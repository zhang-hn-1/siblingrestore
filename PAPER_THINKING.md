# 论文思考（Paper Thinking Notes）

> 更新日期：2026-09-01
> 说明：本文件记录当前论文写作的中文思路草稿，对应英文版 Introduction 草稿（含 [SOTA-PSNR]、[LODO-PSNR] 等占位符，待实验数据填入）。

---

## 一、贡献（Contributions，中文版）

1. 我们提出了一种面向无人机输电线路巡检的源保真一体化恢复方法，该方法在多种恶劣退化条件下，综合考虑了像素重建、实例级源信息保持以及部署效率。

2. 我们提出了一种面向退化的源保真度锚定方法。一个仅用于训练的退化分类器对具有退化判别性的特征表示进行正则化，而一个冻结的验证器则将每个恢复后的图像与其共享的干净源锚点对齐。二者协同工作，在推理过程中无需退化标签、参考图像或辅助网络即可提升源保真度。

3. 我们开发了一种紧凑的单图像恢复网络，并在七种退化条件下，通过一个源域不重叠的 PLAMD 变握（variegrip）协议进行了评估。在当前受控设置下，该方法在保持 263 万模型参数的前提下，相比仅用于重建的相同骨干网络，在 PSNR 上提升了 0.35 dB，在源域检索 Top-1 准确率上提高了 5.19 个百分点。

---

## 二、引言草稿（Introduction，中文版）

无人机已经成为输电线路自动化巡检的重要工具，其能够以低成本、高效率的方式获取复杂环境下的设备图像。然而，实际巡检通常发生于开放环境中，采集图像容易受到雾霾、雨雪、低照度、传感器噪声以及运动模糊等因素影响。这些退化会破坏输电组件的边缘结构和局部纹理，降低后续检测、识别和状态评估的可靠性。现有图像恢复方法通常以像素重建质量作为主要优化目标，例如 PSNR 和 SSIM。然而，对于输电线路巡检而言，仅提高视觉质量并不能完全满足实际需求。恢复后的图像不仅需要接近干净图像，还需要保留原始设备实例的身份信息，使系统能够准确识别同一输电组件。因此，面向巡检任务的图像恢复需要同时考虑重建质量和源身份保持能力。

一体化图像恢复旨在通过单一统一模型处理多种未知退化问题。AirNet 通过对比学习来学习退化特征表示，并利用这些表示指导一个共享的恢复网络 [2]。PromptIR 引入了可学习的提示，以适应不同的退化模式 [3]。InstructIR 基于人类指令进行图像恢复，并采用辅助的意图分类目标 [4]。MPerceiver 利用多模态提示和预训练的扩散先验，以提升模型的适应性和泛化能力 [5]。AdaIR 在频域中挖掘退化线索，并根据推断出的退化情况对恢复特征进行调制 [6]。DFPIR 在训练过程中扰动感知退化的特征，以增强在不同恢复任务间的适应性 [7]。ClearAIR 进一步结合全局质量评估、语义引导和局部退化建模，实现从粗到细的恢复过程 [8]。尽管取得了这些进展，无人机输电线路巡检仍面临三大关键挑战。首先，许多高性能方法依赖于大型主干网络、外部先验知识或迭代采样，限制了其在资源受限的空中平台上的部署。其次，这些方法的目标主要集中在像素级或感知相似性上，未能明确保留实例特有的组件结构。第三，大多数评估侧重于在通用自然图像上观察到的合成退化，对于未见的退化类型、复合噪声、严重程度变化以及跨组件分布变化等情况，提供的证据有限。

PLAMD 数据集提供了一种天然的源对齐恢复场景，其中每一个输电组件均包含一个干净源图像以及对应的多种退化观测结果。这些退化图像共享相同的设备结构和实例信息，同时包含不同成像条件引入的变化。因此，该数据结构为研究恢复过程中如何区分退化因素和源身份信息提供了新的机会。然而，直接约束不同退化图像恢复结果之间的一致性并不能保证身份保持。由于不同退化样本可能包含不同程度的残余噪声和伪影，强制恢复结果之间保持完全一致可能导致错误信息传播，并损害真实结构细节。

基于这一观察，我们认为恢复模型需要一个稳定的源身份约束，而不是简单的退化样本一致性约束。
